{
  "name": "studio-graphene-mini-expense-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack mini expense tracker for the Studio Graphene assessment.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "backend:start": "npm start --prefix backend",
    "backend:dev": "npm run dev --prefix backend",
    "backend:test": "npm test --prefix backend",
    "frontend:start": "npm start --prefix frontend",
    "frontend:build": "npm run build --prefix frontend",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "verify": "npm run backend:test && npm run frontend:build"
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
